org.gradle.workers.max=8

# Build Java only by default; the library is being phased over from C++ for 2026.
# Pass -PjavaOnly=false to build the C++ as well, which is still in the tree.
javaOnly=true
